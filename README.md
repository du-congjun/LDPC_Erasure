# LDPC codes for Erasure Channel
This repo is a simple implementation of LDPC codes for Erasure Binary channels. LDPC codes are Ensemble codes for iteratively reconstructing a codeword when exposed to erasure channels. 

## Dependencies
The script is written is python and sage.


## Example Run
sage -python LDPC.py
